require 'test_helper'

class PiggybakFreeShippingTest < ActiveSupport::TestCase
  # puts note about requiring free_shipping method on product

  # assert fake cart with free shipping items returns available and free
  # assert fake cart with mixed items returns not available and not free
  # assert fake order with free shipping items returns available and free
  # assert fake order with mixed items returns not available and not free
end
